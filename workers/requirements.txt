# KINTEL Workers — runtime dependencies. Python pinned to 3.11 (.python-version + render.yaml PYTHON_VERSION).
# Test-only deps live in requirements-dev.txt (NOT installed on Render).
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
httpx==0.27.2
supabase==2.10.0
langgraph==0.2.60
langchain-core==0.3.28
